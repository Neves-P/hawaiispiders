a_datatables <- rbind(
  cbind(a_c_max_datatable, dataset = factor("a_c_max")),
  cbind(a_c_min_datatable, dataset =  factor("a_c_min")),
  cbind(a_c_no_datatable, dataset = factor("a_c_no")),
  cbind(a_m_max_datatable, dataset = factor("a_m_max")),
  cbind(a_m_min_datatable, dataset = factor("a_m_min")),
  cbind(a_m_no_datatable, dataset = factor("a_m_no"))
)

